var expect = require("chai").expect;
var mob = require("../app/mob");

describe("The mob", () => {
  it("3 returns fizz", () => {
    let expected = "fizz";
    let actual = mob.fizzBuzz();
    expect.equal;
  });
});
